apiVersion: v1
kind: Service
metadata:
  name: example-schemaregistry
  namespace: kafka
spec:
  selector:
    app: example-schemaregistry
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30881
  type: NodePort